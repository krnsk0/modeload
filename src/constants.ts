/**
 * Database key where Cursor stores user settings including custom modes
 */
export const CURSOR_SETTINGS_KEY = 'src.vs.platform.reactivestorage.browser.reactiveStorageServiceImpl.persistentStorage.applicationUser';

/**
 * Property name for composer state in Cursor settings
 */
export const COMPOSER_STATE_KEY = 'composerState';

/**
 * Property name for modes array in composer state
 *
 * May change as Cursor versions update.
 */
export const MODES_KEY = 'modes4';